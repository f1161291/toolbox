services:
  open-webui:
    image: open-webui/open-webui:main
    container_name: open-webui
    ports:
      - "3000:8080"
    environment:
      - OLLAMA_BASE_URL=http://192.168.0.80:11434/v1
    volumes:
      - open-webui:/app/backend/data
    restart: always

volumes:
  open-webui:
